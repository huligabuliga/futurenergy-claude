{
  "name": "futurerp-mcp",
  "version": "2.0.0",
  "description": "Remote MCP server (Streamable HTTP + Supabase OAuth) for querying Future Energy's internal ERP (pronto-resolver on Supabase) \u2014 tickets, leads, installations, drones, KPIs, WhatsApp",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
