# FuturERP MCP — Supabase credentials for pronto-resolver-61
#
# SUPABASE_SERVICE_ROLE_KEY accepts either:
#   - New-style secret key: starts with `sb_secret_...` — create in Supabase Dashboard
#       → Project Settings → API Keys → Publishable and secret API keys → New secret key
#   - Legacy service_role JWT: long eyJ... — Dashboard → API Keys → Legacy anon, service_role API keys
#
# Both have the same privilege (bypass RLS, full DB read). Prefer the new secret keys
# — they can be named, rotated, and revoked individually.
#
# Ask Jonas if you don't have dashboard access.

SUPABASE_URL=https://rczhnuurvcxtkfussmfj.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your_sb_secret_or_service_role_key_here
